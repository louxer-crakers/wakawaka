version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Pre-build phase started"
    build:
      commands:
        - echo "Build phase started"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []

version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Pre-build phase started"
        - echo "No environment variable replacement needed"
        - echo "API configuration will be set via UI Settings"
    build:
      commands:
        - echo "Build phase started"
        - echo "Frontend uses persistent storage for configuration"
        - echo "Build completed successfully"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []